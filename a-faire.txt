il y a un systeme de bonus qu on a dévelopé
dans le systeme en gerale. Toi tu n' auras qu' à afficher le solde bous 
de l' utilisateur connecté sur son tableau de bord, 
tout est déjà implémenté en backend, la route qui retourne 
le dashboard , il contient un nouveau champ : 

Nouveau Champ disponible : 

// Réponse de GET /api/users/{userId}
{
  "solde": 1000,      // Solde normal
  "bonus": 500,       // ← NOUVEAU : Solde bonus temporaire
  "gain": 200
}


Les APIs restent identiques - juste un champ en plus
